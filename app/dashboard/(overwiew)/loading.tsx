//special file of Next to make streaming in the all page while dinamic components are rendered

import DashboardSkeleton from '@/app/ui/skeletons';
export default function Loading() {
  return <DashboardSkeleton />;
}
